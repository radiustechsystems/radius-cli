---
'radius-cli': patch
---

Point the package's `repository`, `homepage` and `bugs` metadata at github.com/radiustechsystems/radius-cli (it named a non-existent `radiustech` org). npm provenance requires `repository.url` to match the repository the package is published from.
