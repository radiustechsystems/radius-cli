---
"radius-sdk": minor
---

Add ERC-20 interactions defaulting to SBC: `getTokenMetadata`, `getAllowance`, `approve`, `transfer`, `transferFrom`, `getTransfers`, `watchTransfers` and the `erc20Actions()` client extension, exported from `radius-sdk/client`; `createRadiusFetch(...)` gains `allowance` and `approve`.
